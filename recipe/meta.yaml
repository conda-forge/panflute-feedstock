{% set name = "panflute" %}
{% set version = "1.12.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 5c05dc65328b5881501fa5c1db4f202a0508c1b2f161c6b843516d0c47a49b95

build:
  noarch: python
  number: 0
  entry_points:
    - panflute = panflute:main
    - panfl = panflute:panfl
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python
    - pip
  run:
    - python
    - pyyaml
    - future
    - shutilwhich
    - click

test:
  imports:
    - panflute
  commands:
    - panfl --help

about:
  home: https://github.com/sergiocorreia/panflute
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'Pythonic Pandoc filters'
  doc_url: http://scorreia.com/software/panflute/
  dev_url: https://github.com/sergiocorreia/panflute

extra:
  recipe-maintainers:
    - kiwi0fruit
